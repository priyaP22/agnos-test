<% @order.order_items.each do |order_item| %>
  <p>
    <strong>Item:</strong>
    <%= order_item.item.name %>
  </p>

  <p>
    <strong>Price:</strong>
    <%= order_item.item.price %>
  </p>

  <p>
    <strong>Quantity:</strong>
    <%= order_item.quantity %>
  </p>
<% end %>